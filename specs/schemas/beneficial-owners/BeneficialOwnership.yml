BeneficialOwnership:
  title: BeneficialOwnership
  type: object
  required:
    - _links
    - status
  properties:
    _links:
      type: object
      properties:
        self:
          $ref: '../../schemas/common.yml#/components/schemas/HalLink'
    status:
      type: string
      enum:
        - uncertified
        - certified
        - recertify
      example: uncertified
